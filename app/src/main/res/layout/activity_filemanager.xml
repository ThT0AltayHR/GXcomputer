<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/gx_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="14dp">

        <ImageView
            android:id="@+id/upButton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="4dp"
            android:src="@drawable/ic_arrow_back"
            android:visibility="invisible"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/currentPathText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:textColor="@color/gx_text_primary"
            android:textSize="15sp"
            android:ellipsize="middle"
            android:singleLine="true"
            tools:text="/GXcomputer" />

        <ImageView
            android:id="@+id/trashViewButton"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:src="@drawable/ic_trash"
            android:contentDescription="@string/file_manager_trash" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/fileRecycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="100dp" />

        <TextView
            android:id="@+id/emptyText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/file_manager_empty_folder"
            android:textColor="@color/gx_text_tertiary"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/trashDropZone"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="20dp"
            android:padding="14dp"
            android:background="@drawable/bg_dock"
            android:src="@drawable/ic_trash"
            android:contentDescription="@string/file_manager_trash" />
    </FrameLayout>

</LinearLayout>
